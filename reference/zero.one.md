# Create a zero one normalization

Create a zero one normalization

## Usage

``` r
zero.one(x)
```

## Arguments

- numeric:

  value

## Value

A standardized 0-1 variable

## Examples

``` r
#rnorm(10) %>% zero.one()

```
