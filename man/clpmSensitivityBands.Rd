% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clpmSensitivityBands.R
\name{clpmSensitivityBands}
\alias{clpmSensitivityBands}
\title{Simulate CLPM sensitivity bands for a reported estimate}
\usage{
clpmSensitivityBands(
  reported_ar,
  reported_cl,
  ar_true = 0.3,
  rho_i = 0.5,
  r_grid = c(0.05, 0.25, 0.5, 0.75, 1, 1.5, 2, 3, 4.5, 6, 9),
  reps = 40,
  n_obs = 1000,
  waves = 4,
  conf = 0.95,
  seed = NULL,
  sim = NULL,
  plot = TRUE,
  style = clpmBandStyle()
)
}
\arguments{
\item{reported_ar, reported_cl}{The published CLPM autoregression and
cross-lag. Drawn as horizontal reference lines.}

\item{ar_true}{True within-person autoregression used to generate data.}

\item{rho_i}{Trait correlation used to generate data.}

\item{r_grid}{Between-to-within variance ratios to sweep.}

\item{reps}{Replications per grid point.}

\item{n_obs}{Persons per replication.}

\item{waves}{Number of waves.}

\item{conf}{Width of the simulation interval, e.g. \code{0.95}.}

\item{seed}{Optional integer seed, set once before the sweep.}

\item{sim}{Optional pre-computed sweep from an earlier call. When supplied
the simulation is skipped entirely.}

\item{plot}{If \code{FALSE}, return the swept data frame instead of a plot.}

\item{style}{A list of visual settings from \code{\link{clpmBandStyle}}.}
}
\value{
A \code{ggplot} object with the swept data attached as the
  \code{"sim"} attribute, or that data frame when \code{plot = FALSE}. The
  data frame carries \code{bw_ratio}, the mean and interval bounds for both
  parameters, and \code{n_obs} / \code{n_miss} per grid point.
}
\description{
Sweeps the between-to-within variance ratio, simulates an RI-CLPM at each
value with a true cross-lag of zero, fits a CLPM to every replication, and
returns the artifact-only curves with simulation intervals. Horizontal
reference lines mark the reported estimates, so the crossings show which
amounts of trait variance could have produced the report with no
within-person effect at all.
}
\details{
The simulation is the expensive part, and it is evaluated lazily: supply a
previously returned \code{sim} data frame (or the \code{"sim"} attribute of
a returned plot) and no data are generated, so a figure can be restyled or
re-faceted without paying for the sweep again.
}
\examples{
\donttest{
p <- clpmSensitivityBands(reported_ar = 0.55, reported_cl = 0.12,
                          reps = 5, n_obs = 300, seed = 1)
# restyle without re-simulating
clpmSensitivityBands(0.55, 0.12, sim = attr(p, "sim"),
                     style = clpmBandStyle(band_fill = "steelblue"))
}
}
