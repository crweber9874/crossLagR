% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clpmBias.R
\name{clpmArtifactTipping}
\alias{clpmArtifactTipping}
\title{Tipping-point trait share for an observed CLPM cross-lag}
\usage{
clpmArtifactTipping(
  observed,
  ar = 0.3,
  cl = 0,
  rho_i = 0.5,
  reliability = 1,
  interval = c(0, 50)
)
}
\arguments{
\item{observed}{Numeric scalar, the observed CLPM cross-lag.}

\item{ar}{True autoregressive parameter (scalar).}

\item{cl}{Numeric vector of hypothesised true within-person cross-lags.}

\item{rho_i}{Trait correlation (scalar).}

\item{reliability}{Indicator reliability (scalar).}

\item{interval}{Search interval for \code{bw_ratio}.}
}
\value{
Data frame with one row per \code{cl}: the tipping-point
  \code{bw_ratio} and implied trait \code{icc}.
}
\description{
Convenience wrapper around \code{clpm_artifact_tipping()}. For each
hypothesised true within-person cross-lag in \code{cl}, returns the
between-to-within variance ratio at which the population CLPM cross-lag
would equal \code{observed}. Useful for "as-if" sensitivity statements
(e.g., "to attribute the observed cross-lag to trait variance alone we
would need r = ..., implying ICC = ...").
}
\examples{
clpmArtifactTipping(
  observed = 0.08, ar = 0.3, cl = c(0, 0.02, 0.05),
  rho_i = 0.5
)
}
