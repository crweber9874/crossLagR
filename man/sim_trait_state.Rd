% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_trait_state.R
\name{sim_trait_state}
\alias{sim_trait_state}
\title{sim_trait_state}
\usage{
sim_trait_state(
  waves = 5,
  n = 1000,
  beta_x = 0,
  beta_y = 0,
  omega_xy = 0,
  omega_yx = 0,
  var_p = 1,
  var_q = 1,
  cov_pq = 0,
  var_BX = 0,
  var_BY = 0,
  cov_BXBY = 0,
  cor_trait_initial = 0
)
}
\arguments{
\item{waves}{Integer. Number of waves (time points). Must be at least 2.}

\item{n}{Integer. Sample size (number of individuals).}

\item{beta_x}{Numeric. Autoregressive effect for X.}

\item{beta_y}{Numeric. Autoregressive effect for Y.}

\item{omega_xy}{Numeric. Cross-lagged effect from X(t-1) to Y(t).}

\item{omega_yx}{Numeric. Cross-lagged effect from Y(t-1) to X(t).}

\item{var_p}{Numeric. Innovation variance for within-person X process.}

\item{var_q}{Numeric. Innovation variance for within-person Y process.}

\item{cov_pq}{Numeric. Innovation covariance between within-person X and Y processes.}

\item{var_BX}{Numeric. Between-person trait variance for X.}

\item{var_BY}{Numeric. Between-person trait variance for Y.}

\item{cov_BXBY}{Numeric. Between-person trait covariance between X and Y.}

\item{cor_trait_initial}{Numeric. Correlation between traits and wave-1
within-person states. Default is 0 (independent, which satisfies the RI-CLPM
identification assumption). Non-zero values violate this assumption and can
reveal bias in models that assume independence. Applied symmetrically:
cor(eta_x, wp_x1) = cor(eta_y, wp_y1) = cor_trait_initial.}
}
\value{
A wide-format data frame with columns \code{x1, ..., x[waves]} and
  \code{y1, ..., y[waves]}.
}
\description{
Simulate panel data from a trait-plus-state data-generating process.
}
\details{
The function simulates two stable traits (between-person random intercept components)
and two within-person AR(1) processes with optional cross-lagged effects. Observed scores
are the sum of trait and state components.

When \code{cor_trait_initial = 0} (default), traits and wave-1 states are drawn
independently. When non-zero, they are drawn jointly from a multivariate normal,
so individuals with higher trait values tend to also have higher (or lower) initial
state values. This violates the RI-CLPM identification constraint that random
intercepts are uncorrelated with wave-1 within-person factors.
}
\examples{
dat <- sim_trait_state(
  waves = 5,
  n = 500,
  beta_x = 0.4,
  beta_y = 0.3,
  omega_xy = 0.1,
  omega_yx = 0.1,
  var_p = 1,
  var_q = 1,
  cov_pq = 0.2,
  var_BX = 0.6,
  var_BY = 0.6,
  cov_BXBY = 0.3
)
head(dat)

}
