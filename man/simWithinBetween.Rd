% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/withinBetween.R
\name{simWithinBetween}
\alias{simWithinBetween}
\title{simWithinBetween}
\usage{
simWithinBetween(wb, n_units, n_waves, n_sims = 500, seed = NULL)
}
\arguments{
\item{wb}{A data frame returned by \code{withinBetween()}.}

\item{n_units}{Integer. Number of units (persons) per simulated panel.
Defaults to a sensible value matching typical panels.}

\item{n_waves}{Integer. Number of waves per simulated panel.}

\item{n_sims}{Integer. Number of bootstrap replicates. Default 500.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A data frame with columns \code{variable}, \code{sim},
  \code{between_var}, \code{within_var}.
}
\description{
Parametric bootstrap of between- and within-person variance
  components implied by a \code{\link{withinBetween}} fit.
}
\details{
For each variable, simulates \code{n_sims} panels from
\eqn{y_{it} = \alpha_i + \varepsilon_{it}} with
\eqn{\alpha_i \sim N(0, \sigma^2_B)} and
\eqn{\varepsilon_{it} \sim N(0, \sigma^2_W)}, then recomputes the empirical
between- and within-person variance components in each replicate. The
resulting distribution describes the sampling variability of the
decomposition under the implied model.
}
