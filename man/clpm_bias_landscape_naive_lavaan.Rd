% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clpmBiasLandscape.R
\name{clpm_bias_landscape_naive_lavaan}
\alias{clpm_bias_landscape_naive_lavaan}
\title{Bias landscape under a naive sequential contemp simulator}
\usage{
clpm_bias_landscape_naive_lavaan(
  ar_seq = seq(0, 0.5, by = 0.1),
  bw_seq = c(0, 0.3, 0.6, 0.9),
  rho_seq = c(0, 0.3, 0.6, 0.9),
  cl_values = c(0, 0.2),
  contemp_xy = 0.2,
  contemp_yx = 0.2,
  waves = 6,
  sample_size = 3000,
  seed = 1
)
}
\arguments{
\item{ar_seq}{Numeric vector of true autoregressions for the x axis.}

\item{bw_seq}{Numeric vector of between-to-within variance ratios for
facet columns.}

\item{rho_seq}{Numeric vector of trait correlations for facet rows.}

\item{cl_values}{Numeric vector of true within-person cross-lags;
becomes an outer facet column so multiple panels appear side-by-side.}

\item{contemp_xy, contemp_yx}{Contemporaneous coefficients.}

\item{waves}{Number of waves per simulated panel.}

\item{sample_size}{Persons per cell.}

\item{seed}{Optional integer seed.}
}
\value{
Data frame with columns \code{ar}, \code{bw_ratio}, \code{rho_i},
  \code{cl}, \code{model} (\code{"CLPM"} or \code{"RI-CLPM"}), and
  \code{cl_bias}.
}
\description{
Same interface as \code{clpm_bias_landscape_lavaan()}, but uses a
deliberately deficient internal simulator (\code{.sim_naive_contemp()})
that can only carry one direction of contemporaneous effect because
standard sequential simulation has no ordering for the bidirectional
loop. The resulting landscape illustrates what an analyst who tried to
bolt contemporaneous structure onto \code{simCLPM()} / \code{simRICLPM()}
without solving the simultaneous system would actually generate.
}
